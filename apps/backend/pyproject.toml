[project]
name = "scoreflow_backend"
version = "0.1.0"
description = "API for scoreflow app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
[dependency-groups]
dev = [
    "pytest",
    "ruff>=0.14.10",
]
[tool.pytest.ini_options]
pythonpath = "."
